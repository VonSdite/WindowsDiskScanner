<Window x:Class="WindowsDiskScanner.App.AiResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 分析"
        Width="850"
        Height="700"
        MinWidth="620"
        MinHeight="480"
        Icon="Assets/app-icon.png"
        Background="#F3F6FA"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,17" Background="#14233C">
            <StackPanel>
                <TextBlock x:Name="TitleText" Foreground="White" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Name="ModelText" Margin="0,4,0,0" Foreground="#AFC2E2" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="20,18" Padding="16" Style="{StaticResource AppCard}">
            <TextBox x:Name="ContentTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="#263751"
                     FontSize="14" />
        </Border>

        <Border Grid.Row="2" Padding="20,14" Background="White" BorderBrush="#E1E7EF" BorderThickness="0,1,0,0">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Margin="0,0,10,0" Content="复制内容" Click="CopyButton_Click" Style="{StaticResource AppButton}" />
                <Button Content="关闭" IsCancel="True" Click="CloseButton_Click" Style="{StaticResource AppPrimaryButton}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
